{
  "name": "@gdp/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch --experimental-strip-types src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.2",
    "@gdp/engines-math": "workspace:*",
    "@gdp/pdf-engine": "workspace:^",
    "fastify": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^26.0.1",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
