import { LightningClient } from './LightningClient';
export const metadata = { title: 'Protección contra Rayos — GroundDesing Pro' };
export default function LightningPage() { return <LightningClient />; }
