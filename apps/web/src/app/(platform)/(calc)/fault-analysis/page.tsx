import { FaultAnalysisClient } from './FaultAnalysisClient';

export const metadata = { title: 'Motor de Análisis de Falla — GroundDesign Pro' };

export default function FaultAnalysisPage() {
  return <FaultAnalysisClient />;
}
