import { VoltagesClient } from './VoltagesClient';
export const metadata = { title: 'Tensiones paso/contacto — GroundDesign Pro' };
export default function VoltagesPage() { return <VoltagesClient />; }
